{{> menu id="dashboard"}}

<section class="ui segment">
  {{#each race}}
      <h2 class="ui header">
        {{series}}
      </h2>
      {{> listrace}}
  {{/each}}

 <p>
  <span id="rating"></span>
</p>
</section>
